import { Role, Status, Responsibility } from './user.enums';

export const roles: string[] = Object.values(Role);
export const userStatuses: string[] = Object.values(Status);
export const responsibilities: string[] = Object.values(Responsibility);