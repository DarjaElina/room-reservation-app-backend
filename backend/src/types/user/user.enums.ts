export enum Role {
  ADMIN = 'ADMIN',
  USER = 'USER',
  MANAGER = 'MANAGER',
}

export enum Responsibility {
  STUDENT = 'STUDENT',
  TEACHER = 'TEACHER',
  BOOKING_AGENT = 'BOOKING_AGENT',
}

export enum Status {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
  DISABLED = 'DISABLED'
}